[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Make sure static files are copied
[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"